# Net Discover Commands

# installation on debian
`sudo apt-get install netdiscover`

# installation on centos
`sudo yum install netdiscover`

# To scan the ip between range
`netdiscover -r 192.168.12.1/24`
